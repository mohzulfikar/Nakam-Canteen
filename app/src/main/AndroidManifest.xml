<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nk2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NK2">
        <activity
            android:name=".contoh"
            android:label="@string/title_activity_contoh"
            android:theme="@style/Theme.NK2.NoActionBar"></activity>
        <activity android:name=".MahasiswaClickToko" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreenStyles">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".AdminBeranda" />
        <activity android:name=".MahasiswaBeranda" />
        <activity android:name=".MahasiswaProfil" />
        <activity android:name=".AdminPengelolaanToko" />
        <activity android:name=".AdminEditToko" />
        <activity android:name=".AdminPengelolaanUser" />
        <activity android:name=".AdminEditUser" />
        <activity android:name=".ShoppingCartActivity" />
        <activity android:name=".AdminRegisterToko" />
    </application>

</manifest>